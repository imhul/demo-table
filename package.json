{
  "name": "demo-table",
  "version": "1.0.0",
  "description": "pricing comparison table",
  "scripts": {
    "go": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "format": "prettier --write src/**/*.{js,css,scss,html,json,md}",
    "predeploy": "rm -rf dist && parcel build src/index.html --no-source-maps --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "author": "Tkachuk Zakhar <blashirk@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "2.10.3",
    "parcel": "^2.10.3",
    "parcel-plugin-prettier": "^0.2.1"
  },
  "dependencies": {
    "gh-pages": "^6.1.0",
    "prettier": "^3.1.0",
    "sass": "^1.69.5"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}
